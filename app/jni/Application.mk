#APP_OPTIM := release
#APP_PLATFORM := android-8

  #APP_ABI := armeabi-v7a
  APP_ABI := all
  APP_CPPFLAGS += -fexceptions
  #APP_OPTIM := debug
  #NDK_DEBUG=1
  APP_STL := c++_static
  APP_OPTIM    := release
  APP_CPPFLAGS := -std=gnu++11